<?xml version="1.0" encoding="utf-8"?>
<menu xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto">


    <item android:id="@+id/search_menu"
        android:icon="@drawable/ic_search_icon"
        app:showAsAction="always"
        android:title="search"
        app:actionViewClass="androidx.appcompat.widget.SearchView" />

    <item android:id="@+id/chats_menu"
        android:icon="@drawable/ic_chat_icon"
        android:title="Chats"/>


    <item android:id="@+id/newGroup_menu"
        android:icon="@drawable/ic_groups_icon"
        android:title="New Group"/>

    <item android:id="@+id/setting_menu"
        android:icon="@drawable/ic_settings_icon"
        android:title="Settings "/>

    <item android:id="@+id/logout_menu"
        android:title="Logout "/>

</menu>